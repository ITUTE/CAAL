.MODEL SMALL
.STACK 100H
.DATA
	M1 DB "NHAP 1 KI TU:$"
	M2 DB 10,13, "MA ASCII THAP PHAN TUONG UNG LA:$"
	M3 DB 10,13, "MA ASCII NHI PHAN TUONG UNG LA:$"
	M4 DB 10,13, "MA ASCII THAP LUC PHAN TUONG UNG LA:$"
	KQ DW 0
	BUFF DB 6
	     DB 0
	     DB 5 DUP(?)
.CODE
START:
MOV AX,@DATA
MOV DS,AX


MOV DX,OFFSET M1
MOV AH,09
INT 21H

MOV AH,0AH
MOV DX,OFFSET BUFF
INT 21H
LDEC:

MOV DX,OFFSET M2
MOV AH,09
INT 21H

MOV BX,OFFSET BUFF
XOR CX,CX
XOR AX,AX
XOR DX,DX
INC BX
MOV CL,[BX]
INC BX
MOV AL,[BX]

;SUB AL,30H
DEC CX
MOV DI,10
CMP CX,0
JZ L2DEC
L1DEC:
INC BX
MUL DI
MOV DL,[BX]

;SUB DL,30H
ADD AX,DX
LOOP L1DEC
L2DEC:
MOV KQ,AX
MOV AX,KQ
MOV DI,10
XOR CX,CX

LCHIA:	
    XOR DX,DX
    DIV DI
    PUSH DX
    INC CX
    CMP AX,0
    JNZ LCHIA

LIN:	
POP DX
XOR DH,DH
ADD DL,30H
MOV AH,02
INT 21H
LOOP LIN
;
LBIN:
;IN LOI NHAC 3
MOV DX,OFFSET M3
MOV AH,09
INT 21H

MOV BX,OFFSET BUFF
XOR CX,CX
XOR AX,AX
XOR DX,DX
INC BX
MOV CL,[BX]
INC BX
MOV AL,[BX]
;SUB AL,30H
DEC CX
MOV DI,10
CMP CX,0
JZ L2BIN
L1BIN:
INC BX
MUL DI
MOV DL,[BX]
;SUB DL,30H
ADD AX,DX
LOOP L1BIN
L2BIN:
MOV KQ,AX
MOV AX,KQ
MOV DI,02
XOR CX,CX
LCHIABIN:	
XOR DX,DX
DIV DI
PUSH DX
INC CX
CMP AX,0
JNZ LCHIABIN
LINBIN:	
POP DX
XOR DH,DH
ADD DL,30H
MOV AH,02
INT 21H
LOOP LINBIN
;
LHEX:
;IN LOI NHAC 4
MOV DX,OFFSET M4
MOV AH,09
INT 21H

MOV BX,OFFSET BUFF
XOR CX,CX
XOR AX,AX
XOR DX,DX
INC BX
MOV CL,[BX]
INC BX
MOV AL,[BX]
;SUB AL,30H
 
 DEC CX
 MOV DI,10
 CMP CX,0
 JZ L2HEX
L1HEX:
INC BX
MUL DI
MOV DL,[BX]
;SUB DL,30H

 ADD AX,DX
 LOOP L1HEX
L2HEX:
      MOV KQ,AX
      MOV AX,KQ
      MOV DI,16
      XOR CX,CX
LCHIAHEX:	
     XOR DX,DX
     DIV DI
     PUSH DX
     INC CX
     CMP AX,0
     JNZ LCHIAHEX
LINHEX:	
    POP DX
    XOR DH,DH
    ADD DL,30H
    MOV AH,02
    INT 21H
    LOOP LINHEX


MOV AH,4CH
INT 21H
END START